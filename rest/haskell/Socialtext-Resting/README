
You will need HTTP 0.4 installed.  Unfortunately, the newest HTTP package
won't work.  If you use a Debian system you can get HTTP 0.4 by installing
libghc6-http-dev

To build the system you can just type "make".  There is a sample program
provided in bin, which you can build by doing:

    ghc --make bin/get.hs -o get
